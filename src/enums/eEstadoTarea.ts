/**
 * Enumeración `EstadoTarea` que define los estados posibles de una tarea.
 * Permite clasificar las tareas según su progreso o finalización.
 */
export enum EstadoTarea {
    PENDIENTE="Pendiente",
    COMPLETADA="Completada"
}