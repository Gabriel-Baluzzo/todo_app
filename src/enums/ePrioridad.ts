/**
 * Enumeración `Prioridad` que define los niveles de prioridad para las tareas.
 * Se utiliza para clasificar las tareas según su importancia o urgencia.
 */
export enum Prioridad {
    ALTA,
    MEDIA,
    BAJA
}